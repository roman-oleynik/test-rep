import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    title: {
      fontSize: 20,
      fontWeight: 'bold',
      lineHeight: 40,
    },
    simpleText: {
      fontSize: 14,
      textAlign: 'center',
      lineHeight: 30,
    },
    logo: {
      height: 200,
      width: 200
    }
  });